# Copy to .env and fill in (see docs/phase-2-design.md, Stage 0 handoff).
# Both are PUBLIC/publishable values (safe to ship); RLS is the real guard.
# Never put the Google client SECRET or the Supabase service/secret key here.
VITE_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
