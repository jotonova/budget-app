# Baked into release/CI builds so installers ship with cloud sync enabled.
# These are PUBLIC-safe: the publishable ("anon") key only permits what RLS
# allows, and the project URL is not a secret. The Google client SECRET and the
# Supabase service/secret key are NOT here and must never be committed.
VITE_SUPABASE_URL=https://mwhqahvqugodzdzyimcp.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_4iHnyKTI717F99dmESNa0g_ewZmjvBx
